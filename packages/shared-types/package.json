{
  "name": "@saas/shared-types",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript interfaces and types across the platform",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "echo 'lint ok'",
    "build": "echo 'types-only package, no build needed'"
  }
}
