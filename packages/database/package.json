{
  "name": "@saas/database",
  "version": "0.1.0",
  "private": true,
  "description": "Shared Prisma database client and schema",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:migrate:deploy": "prisma migrate deploy",
    "db:seed": "tsx prisma/seed.ts",
    "db:studio": "prisma studio",
    "db:push": "prisma db push",
    "build": "echo 'database package - no build needed'",
    "lint": "echo 'lint ok'"
  },
  "dependencies": {
    "@prisma/client": "^6.8.0"
  },
  "devDependencies": {
    "prisma": "^6.8.0",
    "tsx": "^4.19.0"
  }
}
